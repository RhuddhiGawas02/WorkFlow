name: pipeline
on: [push]
jobs:
  build-pipeline:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Hello"
    - run: | 
        dotnet --version
        dotnet restore
        dotnet build --configuration Release AzureFunctionHttp
        echo "Build Successfully"
        
